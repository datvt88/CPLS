# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Gemini AI API Key
GEMINI_API_KEY=your_gemini_api_key

# Zalo OAuth Configuration
NEXT_PUBLIC_ZALO_APP_ID=your_zalo_app_id
ZALO_APP_SECRET=your_zalo_app_secret

# Zalo OAuth Redirect URI (Optional - for testing only)
# By default, auto-detected as: ${window.location.origin}/auth/callback
# Only set this if you need to override for testing
# NEXT_PUBLIC_REDIRECT_URI=https://your-exact-domain.com/auth/callback

# IMPORTANT: Register your redirect URI in Zalo Developer Console:
# - Development: http://localhost:3000/auth/callback
# - Production:  https://yourdomain.com/auth/callback
# Go to: https://developers.zalo.me/ → Your App → OAuth Settings
